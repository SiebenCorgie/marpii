[package]
name = "canvas"
version = "0.1.0"
edition = "2024"

[dependencies]
iced-marpii = { workspace = true, features = ["geometry"] }
iced = { workspace = true, features = ["smol", "canvas"] }
chrono = "0.4.40"
simple_logger.workspace = true
log.workspace = true
