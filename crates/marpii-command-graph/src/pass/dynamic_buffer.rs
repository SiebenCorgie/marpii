///Hosts a buffer that can be updated with new information at runtime via the `update` function.
/// Whenever the pass is scheduled the gpu buffer will be updated with this new information.
pub struct DynamicBufferPass {
    //TODO
}
