///Hosts a image that can be updated with new data at runtime via the `update` function.
/// Whenever the pass is scheduled the gpu image will be updated with this new information.
pub struct DynamicImagePass {
    //TODO
}
