//TODO: - write simple macro that expands to the standard layout and takes the descriptorset binding id as input.
//      - write Bindless::from_set fucntion/macro that takes the references to the image/buffer arrays and returns the wrapper

///Bindless descriptor set representation
struct BindlessDesc {}

impl BindlessDesc {}
